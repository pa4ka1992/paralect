import { Text } from '@mantine/core';
import { FC } from 'react';

export const VacancyBox: FC = () => {
  // const { data } = useSearchVacanciesQuery({ keyword: '', payment_from: '', payment_to: '' });
  return (
    <section>
      <Text>VacancyBox</Text>
    </section>
  );
};
